<?xml version="1.0" encoding="utf-8"?>
<!--
  The library declares what the library needs, and the application inherits it
  by manifest merge. It also has to be here for its own sake: an instrumented
  test runs against this module, and a permission the module does not declare
  cannot be granted to it — which surfaces as "Error granting runtime
  permission" and says nothing about where to look.
-->
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!--
      A transfer runs in the foreground because background work that is not in
      the foreground is killed by OEM battery managers however correct it is.
      A foreground service without a notification is not one, hence the last.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>
        <!--
          Where the Cast SDK looks for its configuration, by name, at startup.
          It names the default media receiver, which needs no registration and
          no fee: what is cast is an ordinary URL a television can fetch.
        -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="dev.stratus.core.cast.StratusCastOptions" />

        <!--
          WorkManager owns the service; what is added here is the type, which
          Android 14 requires and which Play reviews. Merged rather than
          declared, because the class is theirs.
        -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>
</manifest>
